// import api from '../api/api';
const state = {
    basket: [],
};

const getters = {
    getBasket(state) {
        return state.basket;
    },
};

const actions = {
    // addItemToBasket(context, item) {
    // },

};

const mutations = {
    // ADD_ITEM_TO_BASKET_HANDLER(state, item) {
    // },

};

export default {
    state,
    getters,
    actions,
    mutations,
};
